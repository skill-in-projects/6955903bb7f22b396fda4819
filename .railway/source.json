{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && dotnet restore Backend.csproj && dotnet publish Backend.csproj -c Release -o /app/publish"
  },
  "deploy": {
    "startCommand": "dotnet /app/publish/Backend.dll",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
